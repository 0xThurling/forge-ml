#include <functional>
#include <string>
#include <vector>

struct Node {
  double data = 0.0;
  double grad = 0.0;
};

namespace forgeml {
// Stores scalar values and it's gradient
class Value {
};
} // namespace forgeml
